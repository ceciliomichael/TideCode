---
status: draft
---

# Format MCP invocation labels

Goal: show MCP tool invocations as `Ran mcp_name (tool_name)` in the chat presentation. Change only the MCP presentation target/label logic and its focused tests. Verification: run the tool invocation presentation tests and lint the touched files.
