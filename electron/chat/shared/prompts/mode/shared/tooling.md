<shared_tooling_contract>
- Use the tool that gets the best result with the least work.
- Use a fitting MCP tool when it has the real data or action needed.
- Check tool errors and cut-off results.
- Add packages with the project's package tool. Use `npm install` in npm projects.
- Use Kanban only when the user asks or a card is already in use.
</shared_tooling_contract>
