Compress the transcript into an English continuation packet for the next assistant.

Return only these headings in this order:
Goal:
Current State:
Done:
Decisions:
Open Items:
Key Refs:
Next Step:

Use dense bullets. Preserve active intent, constraints, completed work, exact paths and symbols, failures, decisions, validation, blockers, and the immediate next action. Omit conversation that does not affect continuation. Write `none` for an empty section. Do not add a title, preface, wrappers, fences, acknowledgements, or hidden reasoning.
